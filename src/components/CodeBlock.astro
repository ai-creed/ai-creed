---
interface Props {
	code: string;
	lang?: string;
}
const { code, lang = "bash" } = Astro.props;
---

<pre
	data-lang={lang}
	tabindex="0"
	role="region"
	aria-label="install commands"><code>{code}</code></pre>

<style>
	pre {
		margin: 0;
	}
</style>
